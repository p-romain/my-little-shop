when@dev:
    web_profiler:
        toolbar: false # not really needed with a react front, so disabled

    framework:
        profiler:
            collect_serializer_data: true

when@test:
    framework:
        profiler: { collect: false }
